# voice-ai-partner infra (Phase 1)

AWS CDK (TypeScript) project for the Phase 1 backend. Region: `ap-northeast-1`.

## Stacks

- `InfraCoreStack`: DynamoDB short-term memory table, S3 artifacts/logs bucket
- `InfraStack`: health-check Lambda + Function URL, Secrets Manager secrets
  (depends on `InfraCoreStack`)

## Useful commands

* `npm run build`   compile typescript to js
* `npm run watch`   watch for changes and compile
* `npm run test`    perform the jest unit tests
* `npx cdk deploy InfraCoreStack InfraStack --require-approval never`
  deploy both stacks to `ap-northeast-1`
* `npx cdk diff`    compare deployed stack with current state
* `npx cdk synth`   emits the synthesized CloudFormation template

This environment is bootstrapped (`npx cdk bootstrap`), so deploys use the
standard asset-based flow (S3 staging bucket, ECR repo for container
assets, CDK-managed IAM roles) — no special synthesizer needed.

## API authentication (shared secret)

The health-check Function URL (`InfraStack.HealthCheckFunctionUrl` output)
does **not** use `AWS_IAM` auth on the Function URL itself (`AuthType:
NONE`); instead, the Lambda handler checks a shared secret on every
request.

- The secret is generated by CDK and stored in Secrets Manager as
  **`voice-ai-partner-api-shared-secret`**.
- Callers must send it in the **`x-api-secret`** request header.
- Requests with a missing or incorrect header get `401 {"error":
  "unauthorized"}`.

### Reading the secret value

```
aws secretsmanager get-secret-value \
  --secret-id voice-ai-partner-api-shared-secret \
  --region ap-northeast-1 \
  --query SecretString --output text
```

### Example request

```
curl -H "x-api-secret: <secret value>" <HealthCheckFunctionUrl>
```

### Flutter app integration (for the next phase)

When the Flutter client calls this (or any future) backend endpoint, it
must send the shared secret as a request header:

```dart
final response = await http.get(
  Uri.parse(healthCheckUrl),
  headers: {
    'x-api-secret': sharedApiSecret, // fetched via a secure channel, not hardcoded in the app
  },
);
```

Do not hardcode the secret value in the app bundle. For Phase 1 this is
acceptable to pass in via a build-time config injected at CI/CD time, but
before a public release this should be replaced with a per-user/per-device
credential (e.g. Cognito) rather than one static shared secret embedded in
the client.

Secrets Manager supports rotation — if the secret is rotated, any client
holding a stale copy will start getting `401`s, so the distribution
mechanism to the Flutter app needs to account for that (e.g. fetch it from
a config endpoint at app startup rather than baking it into the binary).
